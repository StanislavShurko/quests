#include <stdio.h>

#define arrSize 5

int hIndex(int* papers, int papersSize) {

}

int main() {
  int papers[arrSize] = { 3, 0, 6, 1, 5 };

  printf("%d\n", hIndex(papers, arrSize));

  return 0;
}
